export var type = 'sub'

export var commands = {

}

export default class Json{

// .append(
// 	$("<input>")
// 		.addClass("jsonl")
// 		.prop("type", "file")
// 		.hide()
// 				)
// this.elements.$jsonl[0].addEventListener('chsange', this.readJson, false);
	// readJson(data) {
	// 	var file = data.target.files[0];
	// 	var reader = new FileReader();

	// 	reader.con = $(this).closest(".wind").data("con");
	// 	reader.onload = function (event) {
	// 		try {
	// 			this.con.jsond = JSON.parse(event.target.result);
	// 			this.con.log("JSON loaded", "ok");
	// 		} catch (error) {
	// 			this.con.log(`JSON parsing error: ${error}`, "error");
	// 		}

	// 	}
	// 	reader.readAsText(file, "UTF-8");

	// }
}